# Analysis of Get_NeedRawEXP Implementations in Calculation.cpp (All Versions)

## Overview
The function `Get_NeedRawEXP` calculates the required EXP to reach the next level. There are three main versions in the codebase:
- `src/sho_gs/SHO_GS_LIB/Common/Calculation.cpp`
- `src/common/Calculation.cpp`
- `src/client/Common/Calculation.cpp`

This report compares all three, identifies patterns, and highlights issues and recommendations for handling levels above 189.

---

## 1. Function Signatures
- **sho_gs**: `__int64 CCal::Get_NeedRawEXP(int iLevel)`
- **common**: `int CCal::Get_NeedRawEXP(int iLevel)`
- **client**: `__int64 CCal::Get_NeedRawEXP(int iLevel)`

**Note:** The server and client use 64-bit return types, but the common version uses 32-bit. This could cause overflow for high levels.

---

## 2. Formula Patterns by Level Range
All three versions use piecewise formulas for different level ranges. The formulas are similar, with minor differences in boundaries and constants. Here is a summary:

### A. Low Levels (<= 15)
```cpp
EXP = (LV + 3) * (LV + 5) * (LV + 10) * 0.7
```

### B. Early-Mid Levels (<= 50 or 60)
```cpp
EXP = (LV - 5) * (LV + 2) * (LV + 2) * 2.2
```

### C. Mid Levels (<= 100 or 113)
- Taiwan/Pre-Evo: `(LV - 5) * (LV + 2) * (LV - 38) * 9`
- Korea/Modern: `(LV - 11) * (LV) * (LV + 4) * 2.5`

### D. Higher Levels (<= 139 or 150)
- Taiwan/Pre-Evo: `(LV + 27) * (LV + 34) * (LV + 220)`
- Korea/Modern: `(LV - 31) * (LV - 20) * (LV + 4) * 3.8`

### E. High Levels (<= 189)
```cpp
EXP = (LV - 67) * (LV - 20) * (LV - 10) * 6
```

### F. Very High Levels (> 189)
- **sho_gs/client**: `(LV - 90) * (LV - 120) * (LV - 60) * (LV - 170) * (LV - 188)`
- **common**: No formula for > 189; Taiwan/Pre-Evo stops at 200 with a different formula.

---

## 3. Special Cases
- **Taiwan/Pre-Evo**: Uses different formulas and boundaries, with a hard stop at level 200.
- **Korean/Modern**: Extends to 189, then uses a complex formula for higher levels.
- **Client/Server**: Both use the same formulas and boundaries for modern logic.

---

## 4. Issues Identified
- **Level Cap Handling**: All versions cap `iLevel` at `MAX_LEVEL`.
- **Overflow Risk**: The 32-bit return type in `common` may overflow for high levels.
- **Inconsistent High-Level Handling**: For levels > 189, only the server/client versions have a formula. The common version may not handle these levels properly.
- **Potential for Negative/Zero EXP**: The high-level formula can produce negative or zero values if not carefully checked, especially for levels just above 189.

---

## 5. Recommendations
1. **Unify Return Type**: Use `__int64` for all versions to prevent overflow.
2. **Consistent High-Level Formula**: Adopt the same formula for >189 in all versions, or design a new progression for 190–255.
3. **Boundary Checks**: Ensure the formula for >189 does not return negative or zero values.
4. **Documentation**: Add comments explaining each formula and its intended range.
5. **Testing**: Add unit tests for edge cases (189, 190, 200, 255).

---

## 6. Example Table (sho_gs/client modern logic)
| Level Range | Formula |
|-------------|---------|
| <= 15       | (LV+3)*(LV+5)*(LV+10)*0.7 |
| <= 60       | (LV-5)*(LV+2)*(LV+2)*2.2 |
| <= 113      | (LV-11)*(LV)*(LV+4)*2.5 |
| <= 150      | (LV-31)*(LV-20)*(LV+4)*3.8 |
| <= 189      | (LV-67)*(LV-20)*(LV-10)*6 |
| > 189       | (LV-90)*(LV-120)*(LV-60)*(LV-170)*(LV-188) |

---

## 7. Next Steps
- Decide on the desired EXP curve for 190–255.
- Update all Calculation.cpp/.h files to match.
- Ensure all code paths use the same logic and return type.

---

*Report generated by GitHub Copilot, JDOEBOY*
